# CRC Code — дорожная карта

Живой план. [TASK.md](TASK.md) — исходное ТЗ, оно не правится; здесь фиксируются
принятые решения и расхождения с ним.

## Принятые решения

| # | Решение | Следствие |
|---|---------|-----------|
| 1 | **UI — нативный GPU-рендер** (`wgpu`), путь Zed | Плагины на TS не рисуют интерфейс. Весь UI пишется на Rust, плагины дают логику и вклад в декларативные точки расширения |
| 2 | **Агент — Claude Agent SDK**, а не свой Copilot | Цикл агента, инструменты Read/Write/Edit/Bash/Glob/Grep, права и подагенты уже готовы. Task 3.3 из ТЗ почти отпадает |
| 3 | **BYOK** — юзер логинится своим аккаунтом Anthropic | Токены оплачивает юзер. Риска по API-счетам нет, Redis-лимиты для LLM не нужны |
| 4 | **Extension host — отдельный процесс Node**, не `deno_core` | Claude Agent SDK — Node-библиотека, в голом V8-изоляте она не поднимется. Один рантайм на плагины и на агента; краш плагина не роняет редактор |

## Расхождения с ТЗ

**Фаза 4 меняет предмет продажи.** ТЗ строит Pro-подписку вокруг доступа к ИИ
(серверные ключи, мягкие лимиты, блокировка генерации без подписки). При BYOK
этого предмета нет: токены уже оплачены юзером. Stripe остаётся, но продаёт
редактор — синк настроек, маркетплейс плагинов, интеграции Фазы 5. Задачи 4.1 и
4.2 живут как были, 4.3 (Redis-лимиты на LLM) и 4.4 (блокировка генерации)
теряют смысл в исходном виде.

**В ТЗ нет самого редактирования текста.** Фазы 1–5 покрывают ФС, плагины, ИИ и
биллинг, но ни одной задачи на буфер, подсветку и LSP. Без этого получается чат
с файлами, а не IDE. Добавлена Фаза 1.5.

**`deno_core` из Task 2.1 отложен.** См. решение 4.

## Фазы

### Фаза 1 — ядро (Rust) ✅ 1.1, 1.2

- [x] **1.1** Ядро, многопоточность, состояние приложения → [`crc-core`](crates/crc-core/)
- [x] **1.2** ФС: чтение, запись, поиск по проекту, отслеживание изменений
- [ ] **1.3** Серверное API (Axum) + WebSocket
- [ ] **1.4** PostgreSQL + `sqlx`: пользователи, сессии

### Фаза 1.5 — редактор (добавлено)

- [ ] **1.5.1** Буфер текста: rope, правки, undo/redo, выделения → `crc-text`
- [ ] **1.5.2** Синтаксис: tree-sitter, инкрементальный разбор, подсветка → `crc-syntax`
- [ ] **1.5.3** Рендер: `wgpu`, шейпинг текста, панели и вкладки → `crc-ui`
- [ ] **1.5.4** LSP-клиент: диагностика, переходы, автодополнение → `crc-lsp`

### Фаза 2 — плагины (мост Rust ↔ TS)

- [ ] **2.1** Extension host: процесс Node, JSON-RPC поверх stdio → `crc-plugin`
- [ ] **2.2** Проброс операций ядра в хост (те же, что уже есть в `Engine`)
- [ ] **2.3** TS API для плагинов + документация → `extensions/api`
- [ ] **2.4** Менеджер плагинов: загрузка, изоляция, установка из маркетплейса

### Фаза 3 — агент

- [ ] **3.1** Хранение ключа юзера в keychain ОС, вход через браузер
- [ ] **3.2** Панель чата (нативная) и сессии агента → `crc-agent`
- [ ] **3.3** Инструменты агента поверх `Engine` (песочница уже есть)
- [ ] **3.4** Предпросмотр diff: Accept/Reject, запись через `Engine::write_file`

### Фаза 4 — биллинг

- [ ] **4.1** Stripe: продукты Free и Pro
- [ ] **4.2** Вебхуки Stripe
- [ ] **4.3** Rate limiting на свои сервисы (синк, маркетплейс) — не на LLM
- [ ] **4.4** Гейт по подписке на Pro-функции редактора

### Фаза 5 — интеграции

- [ ] **5.1** Плагин Cloud Design
- [ ] **5.2** Плагин Cloud Code
- [ ] **5.3** Публичное API для комьюнити
- [ ] **5.4** GitHub: OAuth, PR и ревью в редакторе, issues, Actions → плагин на TS

### Фаза 6 — бэклог

- [ ] **6.1** Совместное редактирование — CRDT-слой под буфером → `crc-collab`.
      Кандидаты: `diamond-types`, `loro`, `cola`, `yrs`. Транспорт — WebSocket из 1.3
- [ ] **6.2** Автодополнение — LSP (1.5.4) плюс инлайн-подсказки от агента
- [ ] **6.3** Дизайн-система: иконки, темы, шейпинг шрифтов → часть `crc-ui`
- [ ] **6.4** Мини-Figma — холст с фигурами и дизайн-токенами. См. конфликт ниже
- [ ] **6.5** Git: diff в жёлобе, blame, конфликты, история → `crc-git`

## Что решить до UI

**Мини-Figma плагином конфликтует с решением 1.** Нативный рендер означает, что
TS-плагин не рисует произвольный интерфейс — а холст с фигурами это ровно
произвольный интерфейс. Три выхода: писать её на Rust как встроенную панель;
дать плагинам рисующую поверхность (canvas-API поверх `wgpu`) — это большой
кусок работы и половина преимущества нативного UI теряется; или ограничиться
декларативными точками расширения, и тогда мини-Figma остаётся частью ядра.

**Совместное редактирование дешевле заложить, чем внедрить потом.** Сам rope
переживёт переход, `Edit` и `Selection::shifted` — это уже те примитивы, что
нужны для трансформации. Ломается другое: undo. При совместной работе он должен
быть per-user — откатывать свои правки, а не чужие, чего глобальный стек не
умеет. Страховка стоит недорого: пометить `Transaction` автором сейчас, пока на
историю ничего не завязано.

## Структура

Одна ответственность — один файл; подсистема — отдельный крейт.

```
crates/
  crc-core/     ядро: воркспейс, ФС, поиск, watcher, состояние
  crc-text/     буфер: rope, правки, история, выделения
  crc-syntax/   tree-sitter
  crc-ui/       wgpu-рендер
  crc-lsp/      LSP-клиент
  crc-plugin/   extension host
  crc-agent/    Claude Agent SDK
  crc-server/   Axum + PostgreSQL
  crc-app/      бинарь, связывающий всё
extensions/
  host/         процесс Node: плагины и агент
  api/          TS API для плагинов
```
